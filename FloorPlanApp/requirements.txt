# Core Kivy framework
Kivy==2.3.0

# Optional but commonly used in Kivy apps
Kivy-Garden==0.1.5

# For image handling (used by Kivy)
Pillow>=9.0.0

# For HTTP requests (if your app uses API calls)
requests>=2.31.0

# For data handling (optional)
jsonpickle>=3.0.0

# For file system / multimedia (usually auto-installed, but safe to include)
pygments>=2.14.0
